# Rengmitca IPA Transcription Pipeline — dependencies
#
# Install with:  pip install -r requirements.txt
#
# Python 3.10+ is required.
#
# Conda users should prefer environment.yml for the GPU setup.
# If you are installing with pip on an NVIDIA system, use a PyTorch build
# that matches your CUDA runtime from https://pytorch.org/get-started/locally/.
# The pipeline will fall back to CPU if CUDA is unavailable.

# Deep learning
torch>=2.6.0,<2.9.0
torchaudio>=2.6.0,<2.9.0
transformers>=4.48.0,<5.0.0

# Speaker diarization
pyannote.audio>=3.1.1,<3.2.0

# Audio I/O
soundfile>=0.12.1,<0.13.0
librosa>=0.10.2,<0.11.0

# TextGrid
praatio>=6.2.0,<7.0.0

# Data handling
pandas>=2.2.2,<3.0.0

# Misc
numpy>=1.26.4,<2.1.0
matplotlib>=3.9.0,<3.11.0
